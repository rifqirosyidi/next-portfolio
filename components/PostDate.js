import React from "react";

const Date = ({ children }) => {
  return (
    <p className="font-secondary text-gray-400 font-light  text-md md:text-right">
      {children}
    </p>
  );
};

export default Date;
